server:
  port: 8087
springdoc:
  api-docs:
    path: /groups/v3/api-docs

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/group_service?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    config:
      enabled: false
